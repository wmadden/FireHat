// #define SNAKE_LENGTH 0.25
//
// // snakeSpeed - milliseconds taken to advance one pixel
// int snakeSpeed = 100;
// int snakeTimer = 0;
//
// void snake(CRGBPalette16 palette, CRGB* ledArray, int destArrayLength, int msElapsed) {
//   snakeTimer += msElapsed;
//
//   if (snakeTimer > snakeSpeed) {
//     advanceSnake(palette, ledArray, destArrayLength);
//     snakeTimer = 0;
//   }
// }
//
// void advanceSnake(CRGBPalette16 palette, CRGB* ledArray, int destArrayLength, int msElapsed) {
//   for (int i = destArrayLength; i >= 0; i--) {
//
//   }
// }
